# Windows-specific dependencies for final build

# Core functionality (same as requirements.txt)
psutil>=5.9.0
pystray>=0.19.0
Pillow>=10.0.0

# Windows-only
pyRfactor2SharedMemory     # Shared memory reading (Windows only)
pyinstaller>=6.0.0         # Build .exe (run on Windows)
