# Windows-specific dependencies for monitor
# This file should be installed AFTER requirements.txt

# Note: Core dependencies (psutil, requests, python-socketio) are in requirements.txt
# This file only contains Windows-only packages

# Windows-only packages
# pyRfactor2SharedMemory is vendored in src/pyRfactor2SharedMemory/, not from PyPI
# No additional Windows-only pip packages needed for monitor MVP
