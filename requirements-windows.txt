# Windows-specific dependencies for monitor

# Core functionality (same as requirements.txt)
psutil>=5.9.0                   # Process detection
requests>=2.31.0                # REST API client for LMU setup data
python-socketio[client]>=5.9.0  # WebSocket client for dashboard server

# Windows-only
# Note: pyRfactor2SharedMemory is vendored in src/pyRfactor2SharedMemory/, not from PyPI
# No additional Windows-only packages needed for monitor MVP
